# Minimal HTTP Server

The is a minimal HTTP server implemented in nodejs using the 'net' module.
It is not production ready and is intended for educational purposes only.

Doesn't support streamed requests
